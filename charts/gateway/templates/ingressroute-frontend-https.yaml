{{- $host := .Values.routing.host }}
{{- $tls := .Values.routing.tlsSecret }}

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-ingressroute-front
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ $host }}`) && PathPrefix(`{{ .Values.routing.ingressRouteFront.pathPrefix }}`)
      kind: Rule
      services:
        - name: {{ .Values.routing.ingressRouteFront.service.name }}
          port: {{ .Values.routing.ingressRouteFront.service.port }}
  tls:
    secretName: {{ .Release.Name }}-tls-cert-secret